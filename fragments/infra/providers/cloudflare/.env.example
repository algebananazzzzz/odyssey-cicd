# Copy to .env. The Makefile includes it, so: KEY=value, no quotes or comments.
CLOUDFLARE_API_TOKEN=

# An R2 token for the state bucket. The s3 backend reads no other names.
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
